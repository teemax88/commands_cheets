```python
# Установка модулей в Python || Установка пакета Python
# Пакет можно установить при помощи терминала (командной строки) и pip.
# pip - система управления пакетами, которая используется для установки и управления прогр.пакетами, написан. на Python.
# Установка библиотек python вручную при помощи pip install
# Для установки пакета необходимо выполнить команду:
# pip install имя_пакета или pip install имя_пакета==версия
# Аргумент key позволяет выполнить сортировку по ключу
# Команда pip freeze покажет список уже установленных пакетов
# requirements.txt получение
# Установка пакетов через requirements.txt

```