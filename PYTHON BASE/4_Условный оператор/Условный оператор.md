```python
# ctrl+c останавливаем бесконечный цикл
i = 1
while True:  # бесконечный цикл
    print("Итог итерации = ", i)
    if i == 10:  # закончит цикл (break) когда i = 10, т.е. перейдёт в 8-ю строку
        break
    i += 1
print("Hello")
```
```python
while True:  # вводит от польз. строки и выводит их с их длинной пока не введено exit
    a = input()
    if a == 'exit':
        break
    if len(a) < 5:  # если длинна строки меньше 5-ти возврат в начало цикла (continue) без выполнения того что ниже
        continue
    print(a, len(a))
```

```python
a = [12, 34, 56, 27]  # Yes если в списке все чётные, No если в списке есть нечётное
while len(a) > 0:
    last = a.pop()  # удалит последний элемент в списке перед этим присвоив его в last
    if last % 2 != 0:  # если попался нечётный - напечатает No и выйдет из цикла в 26-ю строку
        print("No")
        break
else:  # напечатает Yes если закончился предидущий цикл и не сработал на нечётность
    print("Yes")
```

## Вложеный условный оператор
```python
a = 35  # в программе очень важны отступы , тогда видно к какому блоку относится if и else и как работает программа
if a % 5 == 0:
    if a > 9 and a < 100:
        print(1)
    else:
        print(2)
else:
    if a % 2 == 0:
        print(3)
    else:
        print(4)
print('END')
```

```python
a, b, c = int(input()), int(input()), int(input())  # программа находит минимальное из 3-х чисел
if a < b:
    if a < c:
        print(a)
    else:
        print(c)
else:
    if b < c:
        print(b)
    else:
        print(c)
```
## Множественный выбор
```python
a = 12
if a > 13:  # если
    print(1)
elif 0 < a < 10:  # ещё если (выполнится если не выполниться строка 2)
    if a % 2 == 0:  # выполнится если выполниться 4я строка
        print('dvuznzchnoe chetnoe')
    else:
        print('dvuznzchnoe nechetnoe')
elif a == 0:  # ещё если (выполнится если не выполниться строка 2,4)
    print(3)
else:  # ещё(тогда) (выполнится если не выполниться строка 2)
    print(4)
    # ответ 4
```