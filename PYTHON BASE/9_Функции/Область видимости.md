```python
# локальная переменная - внутри функции (существует только в функции)
# глобальная переменная - по всей программе (и внутри функции тоже)
# встроенные переменные - те, которые уже есть print, abs, input и т.д

def variable():  # название функции это глобальная переменная
    a, b, c = 1, 2, 4  # локальные переменные
    w = 'локальная'
    print(a, b, c, w, y)  # если есть локальная w то программа будет выводить её,если нет то глобальную

w = 'глобальная'  # глобальные переменные
y = 100  # глобальные переменные
print(w)  # вывод глобальная (переменная глобальная)
variable()  # 1 2 4 локальная 100
```

```python
# global
a = [1, 2, 3]
b = 200
c = 300

def s(a, b, c):  # название функции это глобальная переменная
    # local
    # global a # при такой записи изменения которые были сделаны с а будут изменены и в глобальной а
    a = 30
    # a[1] = 77
    print(a, b, c, 'local')

# s(a, b, c)  # 30 200 300 local переменная а заменена на локальную(если 20я строка вместо 19й [1, 77, 3] 200 300 local)
# s(a, b, c)  # [1, 77, 3] 200 300
print(a, b, c, 'global')  # [1, 2, 3] 200 300 global переменная а взята глобальная

```

```python
def Square(x):
    return x ** 2

input = Square  # встроенная
print(input(3))  # ответ 9 поскольку в input было присвоено результат функции Square(и input перестал вводить с клавы)
```

```python
def example():
    r = 100  # local значение

    def example_in():
        # nonlocal r # если строка активная, то возьмёт r = 'asd'
        r = 'asd'  # enclosing значение
        print(r, t, 'ex_in')  # вывод asd 5 ex_in

    example_in()
    print(r, t, 'test_data')  # вывод 100 5 test_data

r = 333  # global значение
t = 5
example()
```

```python
# ПРИОРИТЕТ В ПОИСКЕ ПЕРЕМЕННОЙ ОТДАЁТСЯ ЛОКАЛЬНОЙ ОБЛАСТИ, т.е если будет с одинак.названием в глобальной области
# и в локальной, то приоритет будет локальной

g = 'Grey global'  # глобальная переменная

def Colors():
    y = 'Yellow'  # локальная переменная которая будет видна только внутри своей функции и вложенным функциям
    g = 'Green local'  # локальная переменная

    def print_red():
        global g  # скажет что g нужно взять глобальную
        nonlocal y  # изменит y на 'New Yellow' не локально, т.е изменит во всей области,но сначала выведет 'Yellow'
        # так как изменения на 'New Yellow' были после print(r, y, g)
        r = 'Red'  # создали локальную переменную которая будет видна только внутри своей функции
        print(r, y, g)
        y = 'New Yellow'

    def print_blue():
        b = 'Blue'  # создали локальную переменную которая будет видна только внутри своей функции
        print(b, y, g)

    # print(r, b) не выведет поскольку r,b локальные переменные только в области видимости своих функций
    print_red()
    print_blue()

# print_red() так вызвать функцию нельзя поскольку она локальная, а не глобальная
Colors()  # Red Yellow Grey global

# Blue New Yellow Green local
```

```python
# можно вызвать подобную функцию и она будет делать то что прописано в параметре, например...
def Colors_1(color='r'):  # если ничего не передать, то примет за 'r'
    def print_red():
        r = 'Red'
        print(r)

    def print_blue():
        b = 'Blue'
        print(b)

    if color == 'r':
        print_red()
    elif color == 'b':
        print_blue()
    else:
        print("I don't know this color ")

Colors_1()  # Red
Colors_1('b')  # Blue
Colors_1('asd')  # I don't know this color
```