```python
for i in range(4):  # for для переменной i с о до 3ки вкл.(итерацией) в for next работает автоматически
    print(i)  # будет выводить 0,1,2,3 через след.строку
    i = 'asdf'  # если оставить так то ничего не поменяется поскольку при возврате в for i станет +1 от предидущей
    print(i)  # 0 asdf 1 asdf 2 asdf 3 asdf вывод такой через перенос
print('OUT', i)  # вывод после цикла OUT и последнее значение i (OUT asdf)

for i in range(100, 1000):  # цикл все 3-х значные числа
    if i % 2 == 0 and i % 7 == 0:  # вывод только тех чисел которые кратны 2-ке и 7-ке
        print(i)

s = 0  # посчитаем и выведем сумму всех чисел от 1 до 100
for i in range(101):
    s = s + i
print(s)

# факториал 5 = !5 = 1*2*3*4*5
a = int(input())  # до какого числа просчитать факториал
pr = 1  # посчитаем и выведем факториал
for i in range(1, a + 1):  # от 1-го поскольку при умножении на 0 итог будет всегда 0. до а+1 чтобы посчитало число а
    pr = pr * i
print(pr)

for i in range(5):  # цикл for будет повторять 5 раз всё что будет в цикле
    print('asd')  # выведет 5 раз asd через перенос

from random import randint

# экспорт из модуля random из функции randint.
b = int(input())  # сколько раз нужен цикл
s = 0
for i in range(b):  # цикл будет a раз
    a = randint(1, 100)  # в переменную а присвоим случайное число от 1 до 100 вкл.
    s += a  # подсчёт суммы случайных чисел
    print(a, end=' ')  # 83,3,74,86,3 вывод случайных чисел через пробел
print()  # перенос на новую строку (поскольку был end и переноса не будет)
print(s)  # сумма всех случайных чисел

for i in range(1, 10):
    print(i, i ** 2)

a = int(input())  # сколько раз будет цикл
s = 0
for i in range(a):
    b = int(input('Введите число '))  # введём число а раз
    s += b
    print('Сумма', s)
print('Сумма итого ', s)
print('Средн.арифм.= ', s / a)

a = int(input())
for i in range(3, 0, -1):
    print(i)  # вывод 3 2 1 например если a = 3

```